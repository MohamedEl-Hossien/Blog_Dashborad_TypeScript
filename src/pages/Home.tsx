import { GetStarted } from "../components";

/**
 * The home page component that renders the introductory content.
 * It displays the `GetStarted` component, which guides users to begin using the app.
 *
 * @component
 * @returns {JSX.Element} The rendered introductory content.
 */
export default function Home() {
  return <GetStarted />;
}
